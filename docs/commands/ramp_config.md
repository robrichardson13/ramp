## ramp config

Configure local preferences for this project

### Synopsis

Configure local preferences defined in the project's ramp.yaml.

Without flags, this command will interactively prompt you to set preferences.

Flags:
  --show   Display current local preferences
  --reset  Delete local preferences (will re-prompt on next command)

```
ramp config [flags]
```

### Options

```
  -h, --help    help for config
      --reset   Delete local preferences
      --show    Display current local preferences
```

### Options inherited from parent commands

```
  -v, --verbose   Show detailed output during operations
  -y, --yes       Non-interactive mode: skip prompts and auto-confirm
```

### SEE ALSO

* [ramp](ramp.md)	 - A CLI tool for managing multi-repo development workflows

